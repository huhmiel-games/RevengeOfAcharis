const enum PlayerState
{
    FALL = 'fall',
    HIT = 'hit',
    IDLE = 'idle',
    JUMP = 'jump',
    DOUBLEJUMP = 'doubleJump',
    JUMP_MOMENTUM = 'momentum',
    MOVE = 'move',
}

export default PlayerState;